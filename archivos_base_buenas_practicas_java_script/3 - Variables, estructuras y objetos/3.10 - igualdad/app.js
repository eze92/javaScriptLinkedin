/*el operador === evalua tipo y valor , es util cuando se necesita validar
una igualdad de contenido , como aca uno es un string y el otro un entero
va devolver false */
console.log(1 === '1')

/*el operador == no evalua tipo y valor  y fuerza la conversion
del mismo,va devolver true */
console.log(1 == '1')
